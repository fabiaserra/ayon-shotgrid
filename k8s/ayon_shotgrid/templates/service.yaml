apiVersion: v1
kind: Service
metadata:
  name: {{ include "ayon_shotgrid.fullname" . }}
  labels:
    {{- include "ayon_shotgrid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ayon_shotgrid.selectorLabels" . | nindent 4 }}
