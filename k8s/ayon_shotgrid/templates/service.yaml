apiVersion: v1
kind: Service
metadata:
  name: {{ include "ayon-shotgrid.fullname" . }}
  labels:
    {{- include "ayon-shotgrid.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ayon-shotgrid.selectorLabels" . | nindent 4 }}
