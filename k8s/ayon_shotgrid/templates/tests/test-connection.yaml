apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "ayon_shotgrid.fullname" . }}-test-connection"
  labels:
    {{- include "ayon_shotgrid.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "ayon_shotgrid.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
